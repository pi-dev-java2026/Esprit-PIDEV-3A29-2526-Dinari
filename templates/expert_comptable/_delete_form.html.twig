<form method="post" action="{{ path('app_expert_comptable_delete', {'id': expert_comptable.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ expert_comptable.id) }}">
    <button class="btn">Delete</button>
</form>
